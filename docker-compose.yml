services:
  db:
    image: postgres:16-alpine
    container_name: mailos-postgres
    ports:
      - '5435:5432'
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=mailos
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
